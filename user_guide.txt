To test the Porgram :

1) Open particules\src  and all *.py  files
2) Execute main.py

3) While program is running, several messages appears in the Console

- Firstly : enter the initial conifg (respecting constraint "." "R" "L")
If your Input repects the constraint, a new message appeaed
else Try again with right format

- Secondly : enter the speed of particules (respecting 0<speed<10)
If your Input repects the constraint, the result of the processing will be printed in the Console
else Try again with right format


